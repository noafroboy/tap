#header
  #logo 
    TAP
  #welcome
    %p
      - if current_user
        = "Hello #{current_user.name}"
        = link_to 'Logout', logout_path, :remote => true, :method => "DELETE", :id => "logout"
      - else
        = link_to 'Login', login_path