<!DOCTYPE html>
<html>
<head>
  <meta http-equiv="Content-type" content="text/html; charset=utf-8" />
  <title>TAP Website</title>
  <%= stylesheet_link_tag :all %>
  <%= javascript_include_tag :defaults %>
  <%= csrf_meta_tag %>
</head>
<body>
  <p>
    <% if current_user %>
      Hello <%= current_user.name %> <%= button_to 'Not you?', logout_path, :confirm => "are you sure?", :method => :delete %>
    <% end %>
  </p>
  
  <%= yield %>
</body>
</html>
